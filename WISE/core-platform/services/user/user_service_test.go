package user

import (
	"testing"

	_ "github.com/proullon/ramsql/driver"
)

func TestList(t *testing.T) {
	/*data.InitLocalDatabase()

	service := &UserDatastore{data.DB}

	user := &UserCreate{FirstName: "Jon", LastName: "Doe"}

	id, err := service.Create(user)
	if err != nil {
		t.Fatalf("an error '%s' occurred", err)
	}

	count, _, err := service.List(0, 10, "", "")

	if err != nil {
		t.Fatalf("an error '%s' occurred", err)
	}

	if count != 1 {
		t.Errorf("Count is not equal to 1: %d", count)
	}

	err = service.Deactivate(id)
	if err != nil {
		t.Fatalf("an error '%s' occurred", err)
	}*/
}
