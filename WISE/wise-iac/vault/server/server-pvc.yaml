apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vault-claim
spec:
  storageClassName: standard
  #storageClassName: hostname 
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
